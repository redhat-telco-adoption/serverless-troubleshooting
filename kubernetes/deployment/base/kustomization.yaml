apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: deployment-

resources:
- deployment.yaml
- service.yaml
- configmap.yaml

labels:
- includeSelectors: true
  pairs:
    app: code-with-quarkus
    app.kubernetes.io/name: code-with-quarkus
    app.kubernetes.io/part-of: quarkus-application
